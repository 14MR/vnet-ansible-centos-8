# Set up 2 vnet on CentOS 8 with Ansible

## Install python dependencies for Ansible
```
pip install -r requirements.txt
```

## Install roles for Ansible
```
ansible-galaxy install -r requirements.yml 
```